import { ObservationMission } from './observation-mission';

describe('ObservationMission', () => {
  it('should create an instance', () => {
    expect(new ObservationMission()).toBeTruthy();
  });
});
