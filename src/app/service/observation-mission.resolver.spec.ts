import { ObservationMission.Resolver } from './observation-mission.resolver';

describe('ObservationMission.Resolver', () => {
  it('should create an instance', () => {
    expect(new ObservationMission.Resolver()).toBeTruthy();
  });
});
